добавление кастомера в библиотеку
    создаем объект класса и добавляем его в библиотеку
добавление книги в библиотеку
    создаем объект класса и добавляем его в библиотеку
сохранение библиотеки в файл(ы)
    делаем первые, тестовые записи
чтение библиотеки из файла
    добиваемся правильного чтения записей из файла

@ дополнения к регистраци книги
поиск максимального значения id книг в библиотеке и генерация нового id для добавленной книги
    пробегаем все записи книг и ищем самое большое значение id, возвращаем его с +1
проверка - сравнение названий книг и имен автора с новой, добавляемой книгой (избегаем дубликатов)
    пробегаем все книги и сравниваем название и автора, если есть совпадения выводим об этом сообщение

@ дополнения к регистраци кастомера
проверка id кастомера, смотрим чтоб в библиотеке такого уже небыло
    пробегаем кастомеров и смотрим их id, если есть совпадение прекращаем регистрацию


# делать новые записи мы уже умеем, теперь будем что-то в них искать

создаем функцию которая получает имя или id, и где искать
    а на выход выдает False если ни чего не нашла
    или список объектов если нашла

добавление контракта        # есть простой и сложный пути
    # простой путь 
    получаем id книги, получаем id кастомера
    по id книги получаем объект и смотрим его тип для определения максимального срока контракта
    по id кастомера проверяем если такой есть в базе
    добавляем контракт с полученными данными

    # сложный путь
    получаем имя или id книги и ищем объекты с таким именем
        если их больше одного, то предлагаем выбрать нужный объект из полученного списка
    получаем имя или id кастомера и опять ищем объекты
        если их больше одного, то предлагаем выбрать нужный объект из полученного списка
    получаем у объекта книги ее тип и формируем дату возврата
    добавляем контракт в библиотеку
    # еще усложнение будет потом

заполняем базу 3-5 книгами и кастомерами, и парой контрактов

@ дополнения к регистрации контракта
 #для простого пути
    после получения id книги ищем ее в базе контрактов, 
        если не находим, то продолжаем как обычно
        если находим, говорим что занята до даты окончания найденого контракта и прерываем процесс регистрации
 # для сложного, если не сказать геморного пути
    объектам книг добавляем пустой параметр id кастомера
    объектам кастомеров добавляем параметр со списком книг
    после получения объекта книги смотрим если параметр id кастомера пустой
        если пустой, продолжаем
        если не пустой ищем контракт и возвращаем дату возврата
    после получения объекта кастомера смотрим если у него контракты
        если есть, проверяем если есть просроченные
            если находим, предлагаем прекратить процесс для этого нехорошего человека

создаем функцию выведения данных базы на экран
    на вход требует тип данных, которые нужно отобразить

закрытие контракта
    получаем имя или id книги, ищем по id полученного объекта запись в контрактах
        если путь был сложно геморный, то очищаем у книги id кастомера, а у кастомера удаляем id книги из списка книг
    удаляем запись контракта из базы

удаление книги
    при простых контрактах находим объект и удаляем его, 
        можно так же поискать ее контракт и удальть его тоже
    при сложных контрактах ищем объект и связанный с ним котракт
        если есть контракт, закрываем его и только потом удаляем книгу

удаление кастомера
    при простых контрактах находим объект и удаляем его, 
        можно так же поискать его контракты и удальть их тоже
    при сложных контрактах ищем объект и связанный с ним котракты
        если есть контракты, закрываем их и только потом удаляем кастомера и возможно еще и книги, которые у него были