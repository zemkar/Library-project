функция чтения из файлов
функция записи в файл
функция добавления кастомера
    проверить что такого ИД нет в базе
функция добавления книги
    проверить что книги с таким названием и автором нет в базе
    присвоить ИД книги, на 1 больше самого высокого значения из базы
функция поиска кастомера
    отдельно по имени и отдельно по ИД
функция поиска книги
    отдельно по имени и отдельно по ИД
функция добавления контракта
    найти кастомера
    найти книгу
    привязать книгу к пользователю
функция закрытия контракта
    найти кастомера
    найти привязанные к нему книги
    если таких больше одной, уточнить какую именно
    если одна, то спросить подверждения
    отвязать книгу от кастомера
функция удаления книги
    найти книгу
    найти связанный с ней контракт
    если нет контракта, то удаляем
    если есть, запрашиваем удаление контракта иначе прерываем
    ищем пользователя по ИД из контракта и закрываем контракт
    после закрытия контракта удаляем книгу
функция удаления кастомера
    найти кастомера
    проверить есть ли за ним книги
    если нет, то просто удаляем
    если есть то запрашиваем или их возврат, или их удаление
    когда у кастомера не будет книг, удаляем его
функция выведения списка записей
    в зависимости от запроса выводим нужный список



проверить что при возврате всех книг у кастомера в поле книги будет пустая СТРОКА в листе, а не пустой лист